version: 2.1
orbs:
  # orgs contain basc recipes and reproducible actions (install node, aws, etc.)
  node: circleci/node@5.0.2
jobs:
  build:
    docker:
      - image: "cimg/base:stable"
      # more setup needed for aws, node, elastic beanstalk
    steps:
      - node/install:
          node-version: "16.16"
      - checkout
      - run:
          name: Install packages
          # TODO: Install, build, deploy in both apps
          command: |
            npm install
      - run:
          name: Install packages
          # TODO: Install, build, deploy in both apps
          command: |
            npm run build

  deploy:
    docker:
      - image: "cimg/base:stable"
      # more setup needed for aws, node, elastic beanstalk
    steps:
      - node/install:
          node-version: "16.16"
      - checkout
      - run:
          name: Configure git
          # TODO: Install, build, deploy in both apps
          command: |
            npm install git config --global user.name "Aly" && git config --global user.email "el7amrawy@outlook.com"
      - run:
          name: Deploy
          # TODO: Install, build, deploy in both apps
          command: |
            npm install && npm run deploy

workflows:
  install and deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
